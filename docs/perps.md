# Perps

Perps REST mutation endpoints are not treated as stable in this SDK. Query helpers are present for markets and positions. Open/close mutation methods return explicit unsupported errors instead of guessing undocumented API contracts.

Perps are high risk. Validate leverage, collateral, max positions, liquidation price, and execution latency before any on-chain integration.

