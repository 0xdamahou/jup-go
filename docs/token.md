# Token

Base: `https://lite-api.jup.ag/tokens/v2`.

Implemented:

- `Search`
- `ByTag`
- `Category`
- `Recent`

Use mint addresses as identity. Symbol and name are display metadata only. Surface audit/risk fields such as suspicious-token indicators when building user-facing flows.

